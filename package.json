{
  "name": "aso-skill-agent-skill",
  "version": "1.5.0",
  "private": true,
  "description": "An installable agent skill for the ASO Skill public API",
  "type": "module",
  "scripts": {
    "check": "node --check test/*.mjs && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
