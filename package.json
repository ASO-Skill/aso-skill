{
  "name": "aso-skill-agent-plugin",
  "version": "1.8.0",
  "private": true,
  "description": "A portable Agent Plugin and Agent Skill for the ASO Skill public API",
  "type": "module",
  "scripts": {
    "check": "node --check test/*.mjs && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
